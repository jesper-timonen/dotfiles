# Reference: https://github.com/i3/i3/blob/next/etc/config
config-version = 2

start-at-login = true

# In i3, all workspaces are phantom
persistent-workspaces = []

after-startup-command = [
  'exec-and-forget borders active_color=0xff00a0ff inactive_color=0xff494d64 width=8.0'
]

# i3 doesn't have "normalizations" feature that why we disable them here.
# But the feature is very helpful.
# Normalizations eliminate all sorts of weird tree configurations that don't make sense.
# Give normalizations a chance and enable them back.
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# Mouse follows focus when focused monitor changes
# on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

[gaps]
inner.horizontal = 6
inner.vertical = 6
outer.bottom = 4
outer.top = 4
outer.left = 4
outer.right = 4

[mode.main.binding]
    # i3 wraps focus by default
    alt-h = 'focus --boundaries-action wrap-around-the-workspace left'
    alt-j = 'focus --boundaries-action wrap-around-the-workspace down'
    alt-k = 'focus --boundaries-action wrap-around-the-workspace up'
    alt-l = 'focus --boundaries-action wrap-around-the-workspace right'

    alt-shift-h = 'move left'
    alt-shift-j = 'move down'
    alt-shift-k = 'move up'
    alt-shift-l = 'move right'

    alt-f = 'fullscreen'
    alt-e = 'layout tiles horizontal vertical' # 'layout toggle split' in i3

    alt-space = 'layout floating tiling' # 'floating toggle' in i3

    alt-1 = 'workspace 1'
    alt-2 = 'workspace 2'
    alt-3 = 'workspace 3'
    alt-4 = 'workspace 4'
    alt-5 = 'workspace 5'
    alt-6 = 'workspace 6'

    alt-shift-1 = 'move-node-to-workspace 1'
    alt-shift-2 = 'move-node-to-workspace 2'
    alt-shift-3 = 'move-node-to-workspace 3'
    alt-shift-4 = 'move-node-to-workspace 4'
    alt-shift-5 = 'move-node-to-workspace 5'
    alt-shift-6 = 'move-node-to-workspace 6'

    alt-d = 'move-workspace-to-monitor next --wrap-around'
    alt-s = 'move-workspace-to-monitor prev --wrap-around'

    alt-shift-r = 'flatten-workspace-tree' # Reset windows on current workspace

    alt-r = 'mode resize'

    alt-shift-s = 'exec-and-forget screencapture -i -c' # Screenshot

[mode.resize.binding]
    h = 'resize width -50'
    j = 'resize height +50'
    k = 'resize height -50'
    l = 'resize width +50'
    enter = 'mode main'
    esc = 'mode main'

[workspace-to-monitor-force-assignment]
    1 = 'main'
    2 = 'main'
    3 = 'secondary'
    4 = 'secondary'
